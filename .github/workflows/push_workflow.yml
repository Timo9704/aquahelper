name: Integration

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "main" ]
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  integration:
    uses: ./.github/workflows/integration.yml